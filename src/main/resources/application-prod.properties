# Production Server Profile
server.port=${PORT:8080}

# Neon PostgreSQL Cloud Database Connection
spring.datasource.url=jdbc:postgresql://ep-plain-butterfly-auj7m4pi.c-10.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_6B3LCvJqZbpK
spring.datasource.driver-class-name=org.postgresql.Driver

# Security Hardening
spring.h2.console.enabled=false

# PostgreSQL JPA / Hibernate Engine Settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# Production Multipart Storage
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
